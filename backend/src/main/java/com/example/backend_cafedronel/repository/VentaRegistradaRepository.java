package com.example.backend_cafedronel.repository;

import com.example.backend_cafedronel.model.VentaRegistrada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VentaRegistradaRepository extends JpaRepository<VentaRegistrada, Integer> {
}
